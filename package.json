{
  "name": "kadoflow",
  "type": "module",
  "private": true,
  "scripts": {
    "tokens:fluid": "node scripts/generate-fluid.mjs",
    "images:build": "node scripts/optimize-images.mjs",
    "predev": "npm run tokens:fluid",
    "pregenerate": "npm run tokens:fluid && npm run images:build",
    "build": "npm run tokens:fluid && npm run images:build && nuxt build",
    "dev": "nuxt dev --host",
    "preview:tunnel": "npx --yes localtunnel --port 3000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "previews": "node scripts/capture-previews.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.14.0",
    "@phosphor-icons/vue": "^2.2.1",
    "gsap": "^3.15.0",
    "lenis": "^1.3.26",
    "nuxt": "^4.5.2",
    "ogl": "^1.0.11",
    "three": "^0.185.1",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/three": "^0.185.4",
    "playwright-core": "^1.62.1",
    "sharp": "^0.35.4",
    "tailwindcss": "^4.3.3"
  }
}
